server:
  port: ${SERVER_PORT:8080}
spring:
  r2dbc:
    url: ${DATABASE_URL:r2dbc:postgresql://localhost:5432/cleanarchitecture}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:postgres}
